<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Profile | Clothes Swap</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Global Design System -->
  <link href="/css/global-design.css" rel="stylesheet">

  <style>
    /* Match profile.ejs vibe */
    .page-header {
      margin-top: 2rem;
      margin-bottom: 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .page-title {
      font-size: 1.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin: 0;
    }

    .page-subtitle {
      font-size: 0.92rem;
      color: #6b7280;
      font-weight: 500;
      margin-top: 0.35rem;
    }

    .pill-link {
      border-radius: 999px;
      padding: 0.4rem 1rem;
      font-size: 0.8rem;
      border: 1px solid rgba(45, 90, 79, 0.2);
      background-color: white;
      color: var(--primary);
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s ease;
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
    }
    .pill-link:hover {
      background: linear-gradient(135deg, rgba(45, 90, 79, 0.1) 0%, rgba(74, 124, 107, 0.1) 100%);
      border-color: var(--primary);
      color: var(--primary);
      text-decoration: none;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(45, 90, 79, 0.15);
    }

    .pill-link-red {
      border-radius: 999px;
      padding: 0.45rem 1.1rem;
      font-size: 0.82rem;
      border: none;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      color: white;
      font-weight: 700;
      transition: all 0.2s ease;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
    }
    .pill-link-red:hover {
      transform: translateY(-2px);
      color: white;
      text-decoration: none;
      box-shadow: 0 6px 16px rgba(45, 90, 79, 0.25);
    }

    .card-soft {
      border-radius: 18px;
      border: 1px solid rgba(45, 90, 79, 0.12);
      background: rgba(255,255,255,0.9);
      box-shadow: 0 8px 24px rgba(45, 90, 79, 0.08);
      overflow: hidden;
    }

    .card-soft .card-header {
      background: linear-gradient(135deg, rgba(45, 90, 79, 0.08) 0%, rgba(74, 124, 107, 0.08) 100%);
      border-bottom: 1px solid rgba(45, 90, 79, 0.12);
      padding: 1rem 1.25rem;
    }

    .card-title {
      font-weight: 900;
      color: var(--secondary);
      margin: 0;
      font-size: 1.05rem;
    }

    .hint-text {
      font-size: 0.85rem;
      color: #6b7280;
      font-weight: 500;
      margin-top: 0.25rem;
    }

    .form-control, .form-select {
      border-radius: 12px;
      border-color: rgba(45, 90, 79, 0.18);
    }

    .form-control:focus, .form-select:focus {
      border-color: rgba(45, 90, 79, 0.45);
      box-shadow: 0 0 0 .25rem rgba(45, 90, 79, 0.12);
    }

    .profile-photo {
      width: 96px;
      height: 96px;
      border-radius: 999px;
      object-fit: cover;
      border: 4px solid var(--primary);
      background-color: #fff;
      box-shadow: 0 8px 24px rgba(45, 90, 79, 0.22);
    }

    .profile-initial {
      width: 96px;
      height: 96px;
      border-radius: 999px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.1rem;
      font-weight: 900;
      border: 4px solid white;
      box-shadow: 0 8px 24px rgba(45, 90, 79, 0.22);
    }

    .alert-message {
      border-radius: 12px;
      border: none;
      font-weight: 500;
    }
    .alert-success {
      background: linear-gradient(135deg, #d1e7dd 0%, #c3e6cb 100%);
      color: #0f5132;
      border-left: 4px solid #198754;
    }
    .alert-danger {
      background: linear-gradient(135deg, #f8d7da 0%, #f5c2c7 100%);
      color: #842029;
      border-left: 4px solid #dc3545;
    }
    .alert-info {
      background: linear-gradient(135deg, #cfe2ff 0%, #b6d4fe 100%);
      color: #084298;
      border-left: 4px solid #0d6efd;
    }

    .danger-zone {
      border-radius: 18px;
      border: 1px solid rgba(220, 38, 38, 0.22);
      background: rgba(255, 255, 255, 0.92);
      box-shadow: 0 8px 24px rgba(220, 38, 38, 0.08);
      overflow: hidden;
    }

    .danger-zone .card-header {
      background: linear-gradient(135deg, rgba(248, 215, 218, 0.85) 0%, rgba(245, 194, 199, 0.85) 100%);
      border-bottom: 1px solid rgba(220, 38, 38, 0.18);
    }

    /* Frutiger Metro Dynamic Background (same as profile.ejs) */
    body {
      background: linear-gradient(45deg, #E8F4F8, #F0E8F8, #F8F0E8, #E8F8F0, #F8F0F0, #E8F4F8);
      background-size: 400% 400%;
      animation: metro-flow 30s ease infinite;
      position: relative;
      min-height: 100vh;
      overflow-x: hidden;
      color: #333;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background-image:
        linear-gradient(90deg, rgba(0, 120, 215, 0.12) 0%, transparent 30%, transparent 70%, rgba(0, 120, 215, 0.12) 100%),
        linear-gradient(180deg, rgba(255, 140, 0, 0.08) 0%, transparent 40%, transparent 60%, rgba(255, 140, 0, 0.08) 100%),
        linear-gradient(45deg, rgba(107, 142, 35, 0.1) 0%, transparent 50%, transparent 50%, rgba(107, 142, 35, 0.1) 100%);
      animation: metro-grid 20s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    body::after {
      content: '';
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background-image:
        linear-gradient(90deg, transparent 0%, rgba(0, 120, 215, 0.05) 25%, rgba(0, 120, 215, 0.05) 75%, transparent 100%),
        linear-gradient(0deg, transparent 0%, rgba(255, 140, 0, 0.04) 50%, transparent 100%);
      background-size: 200% 100%, 100% 200%;
      animation: metro-slide 25s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes metro-flow {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    @keyframes metro-grid {
      0%, 100% { transform: scale(1) translateY(0); filter: brightness(1); }
      25% { transform: scale(1.02) translateY(-5px); filter: brightness(1.03); }
      50% { transform: scale(1.01) translateY(-10px); filter: brightness(1.05); }
      75% { transform: scale(1.02) translateY(-5px); filter: brightness(1.03); }
    }

    @keyframes metro-slide {
      0%, 100% { background-position: 0% 0%, 0% 0%; }
      50% { background-position: 100% 0%, 0% 100%; }
    }

    body > * {
      position: relative;
      z-index: 1;
    }
  </style>
</head>

<body>

<!-- NAVBAR (match profile.ejs) -->
<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid px-4">
    <a class="navbar-brand" href="/">Clothes <span class="brand-red">Swap</span></a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0" style="display: flex; align-items: center; gap: 0.8rem;">
        <li class="nav-item"><a class="nav-link" aria-current="page" href="/">Home</a></li>

        <% if (user) { %>
          <li class="nav-item"><a class="nav-link" href="/inbox">Inbox</a></li>
          <li class="nav-item"><a class="nav-link" href="/wardrobe">Wardrobe</a></li>
          <li class="nav-item"><a class="nav-link" href="/swap">Swap</a></li>
          <li class="nav-item"><a class="nav-link" href="/sahm">SwapMates</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
          <li class="nav-item ms-lg-2">
            <a class="btn btn-sm"
               style="background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; border: none; border-radius: 10px; font-weight: 700; padding: 0.7rem 1.6rem; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(45, 90, 79, 0.2); font-size: 0.95rem; font-family: 'Cinzel', serif; letter-spacing: 0.08em; text-transform: uppercase; display: flex; align-items: center;"
               href="/logout"
               onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 6px 16px rgba(45, 90, 79, 0.3)';"
               onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 4px 12px rgba(45, 90, 79, 0.2)';">
              Logout
            </a>
          </li>
        <% } else { %>
          <li class="nav-item ms-lg-2">
            <a class="btn btn-sm"
               style="background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; border: none; border-radius: 10px; font-weight: 700; padding: 0.7rem 1.6rem; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(45, 90, 79, 0.2); font-size: 0.95rem; font-family: 'Cinzel', serif; letter-spacing: 0.08em; text-transform: uppercase; display: flex; align-items: center;"
               href="/login"
               onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 6px 16px rgba(45, 90, 79, 0.3)';"
               onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 4px 12px rgba(45, 90, 79, 0.2)';">
              Sign In
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div class="container py-4">

  <!-- Flash messages -->
  <% if (typeof success_msg !== 'undefined' && success_msg && success_msg.length > 0) { %>
    <div class="alert alert-success alert-message"><%= success_msg %></div>
  <% } %>
  <% if (typeof error_msg !== 'undefined' && error_msg && error_msg.length > 0) { %>
    <div class="alert alert-danger alert-message"><%= error_msg %></div>
  <% } %>
  <% if (typeof message !== 'undefined' && message) { %>
    <div class="alert alert-info alert-message"><%= message %></div>
  <% } %>

  <!-- Header -->
  <div class="page-header">
    <div>
      <h1 class="page-title">Edit Profile</h1>
      <div class="page-subtitle">Update your profile photo, username, and bio.</div>
    </div>
    <div class="d-flex gap-2">
      <a href="/profile" class="pill-link">← Back to Profile</a>
      <% if (profileUser && profileUser.user_id) { %>
        <a href="/users/<%= profileUser.user_id %>" class="pill-link">View public profile</a>
      <% } %>
    </div>
  </div>

  <div class="row g-4">
    <!-- Left: profile preview -->
    <div class="col-12 col-lg-4">
      <div class="card card-soft">
        <div class="card-header">
          <div class="card-title">Profile Preview</div>
          <div class="hint-text">This is how others will see you.</div>
        </div>

        <div class="card-body p-4 text-center">
          <% const p = profileUser || {}; %>

          <div class="d-flex justify-content-center mb-3">
            <% if (p.profile_image_url) { %>
              <img id="profilePreviewImg" src="<%= p.profile_image_url %>" alt="Profile" class="profile-photo">
            <% } else { %>
              <div id="profilePreviewInitial" class="profile-initial">
                <%= (p.username ? p.username.charAt(0).toUpperCase() : (user && user.username ? user.username.charAt(0).toUpperCase() : '?')) %>
              </div>
              <img id="profilePreviewImg" src="" alt="Profile" class="profile-photo d-none">
            <% } %>
          </div>

          <div style="font-weight:900; font-size:1.1rem; color: var(--secondary);">
            @<span id="usernamePreview"><%= p.username || (user ? user.username : '') %></span>
          </div>
          <div class="hint-text mt-1">
            <%= p.full_name ? p.full_name : 'No full name set' %>
          </div>

          <hr class="my-4" style="border-color: rgba(45, 90, 79, 0.12);">

          <label class="form-label fw-bold text-start w-100">Change profile picture</label>
          <input
            type="file"
            class="form-control"
            name="profile_image"
            id="profile_image"
            accept="image/*"
            form="editProfileForm"
          >
          <div class="hint-text text-start">
            Upload a new image to replace your current one.
          </div>
        </div>
      </div>
    </div>

    <!-- Right: edit form -->
    <div class="col-12 col-lg-8">
      <div class="card card-soft">
        <div class="card-header">
          <div class="card-title">Profile Details</div>
          <div class="hint-text">Keep your account clean and recognizable.</div>
        </div>

        <div class="card-body p-4 p-md-5">
          <form id="editProfileForm" action="/profile/edit" method="POST" enctype="multipart/form-data">
            <div class="row g-3">

              <div class="col-12 col-md-6">
                <label class="form-label fw-bold">Username</label>
                <input
                  type="text"
                  class="form-control"
                  name="username"
                  id="username"
                  value="<%= (profileUser && profileUser.username) ? profileUser.username : '' %>"
                  maxlength="30"
                  required
                >
                <div class="hint-text">This shows on posts, likes, swaps and chats.</div>
              </div>

              <div class="col-12 col-md-6">
                <label class="form-label fw-bold">Email (Locked)</label>
                <input
                  type="email"
                  class="form-control"
                  value="<%= (profileUser && profileUser.email) ? profileUser.email : '' %>"
                  disabled
                >
                <div class="hint-text">Email is locked to avoid breaking login/OTP flows.</div>
              </div>

              <div class="col-12 col-md-6">
                <label class="form-label fw-bold">Full name</label>
                <input
                  type="text"
                  class="form-control"
                  name="full_name"
                  value="<%= (profileUser && profileUser.full_name) ? profileUser.full_name : '' %>"
                  maxlength="80"
                >
              </div>

              <div class="col-12 col-md-6">
                <label class="form-label fw-bold">Gender</label>
                <select class="form-select" name="gender">
                  <option value="" <%= (!profileUser || !profileUser.gender) ? 'selected' : '' %>>Prefer not to say</option>
                  <option value="male" <%= (profileUser && profileUser.gender === 'male') ? 'selected' : '' %>>Male</option>
                  <option value="female" <%= (profileUser && profileUser.gender === 'female') ? 'selected' : '' %>>Female</option>
                  <option value="other" <%= (profileUser && profileUser.gender === 'other') ? 'selected' : '' %>>Other</option>
                </select>
              </div>

              <div class="col-12">
                <label class="form-label fw-bold">Bio</label>
                <textarea
                  class="form-control"
                  name="bio"
                  rows="4"
                  maxlength="280"
                  placeholder="Write something about your style, sizes, swap preferences..."
                ><%= (profileUser && profileUser.bio) ? profileUser.bio : '' %></textarea>
                <div class="hint-text">Max 280 characters.</div>
              </div>
            </div>

            <div class="d-flex flex-wrap gap-2 mt-4">
              <button type="submit" class="pill-link-red">Save changes</button>
              <a href="/profile" class="pill-link">Cancel</a>
            </div>
          </form>
        </div>
      </div>

      <!-- Danger Zone: delete profile -->
      <div class="card danger-zone mt-4">
        <div class="card-header">
          <div class="card-title" style="color:#842029;">Delete Profile</div>
          <div class="hint-text" style="color:#842029;">
            This will remove your profile info and disable your login. It’s irreversible.
          </div>
        </div>

        <div class="card-body p-4">
          <div class="hint-text" style="color:#842029;">
            To confirm, type <b>DELETE</b> and submit.
          </div>

          <form action="/profile/delete" method="POST" class="mt-3"
                onsubmit="return confirm('Are you sure? This cannot be undone.');">
            <div class="row g-2 align-items-end">
              <div class="col-12 col-md-7">
                <label class="form-label fw-bold" style="color:#842029;">Type DELETE</label>
                <input type="text" class="form-control" name="confirm_text" placeholder="DELETE" required>
              </div>
              <div class="col-12 col-md-5 d-grid">
                <button type="submit" class="btn btn-danger" style="border-radius: 14px; font-weight: 800; padding: 0.7rem 1rem;">
                  Delete my profile
                </button>
              </div>
            </div>
          </form>

          <div class="hint-text mt-3" style="color:#842029;">
            Note: This is a safe profile deletion (anonymize + logout) so your swaps/chats don’t break.
          </div>
        </div>
      </div>

    </div>
  </div>

</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<script>
  // Live preview for avatar + username (frontend only)
  (function () {
    const imgInput = document.getElementById('profile_image');
    const img = document.getElementById('profilePreviewImg');
    const initial = document.getElementById('profilePreviewInitial');
    const usernameInput = document.getElementById('username');
    const usernamePreview = document.getElementById('usernamePreview');

    if (usernameInput && usernamePreview) {
      usernameInput.addEventListener('input', function () {
        usernamePreview.textContent = (usernameInput.value || '').trim() || 'user';
        if (initial) {
          initial.textContent = usernamePreview.textContent.charAt(0).toUpperCase();
        }
      });
    }

    if (imgInput && img) {
      imgInput.addEventListener('change', function () {
        const file = imgInput.files && imgInput.files[0];
        if (!file) return;

        const url = URL.createObjectURL(file);
        img.src = url;
        img.classList.remove('d-none');
        if (initial) initial.classList.add('d-none');
      });
    }
  })();
</script>

<!-- AI Chatbot Widget -->
<%- include('ai-chatbot-widget') %>

</body>
</html>
