<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>OOTD Post | Clothes Swap</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Global Design System -->
  <link href="/css/global-design.css" rel="stylesheet">

  <style>
    :root {
      --primary: #2D5A4F;
      --primary-dark: #1F3D34;
      --secondary: #1A1A1A;
      --accent: #4A7C6B;
      --neutral-light: #F5EEE6;
      --border-light: #E8DED1;
    }

    body {
      background: linear-gradient(45deg, #E8F4F8, #F0E8F8, #F8F0E8, #E8F8F0, #F8F0F0, #E8F4F8);
      background-size: 400% 400%;
      animation: metro-flow 30s ease infinite;
      position: relative;
      min-height: 100vh;
      overflow-x: hidden;
      color: #333;
    }
    
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: 
        linear-gradient(90deg, rgba(0, 120, 215, 0.12) 0%, transparent 30%, transparent 70%, rgba(0, 120, 215, 0.12) 100%),
        linear-gradient(180deg, rgba(255, 140, 0, 0.08) 0%, transparent 40%, transparent 60%, rgba(255, 140, 0, 0.08) 100%),
        linear-gradient(45deg, rgba(107, 142, 35, 0.1) 0%, transparent 50%, transparent 50%, rgba(107, 142, 35, 0.1) 100%);
      animation: metro-grid 20s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    body::after {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: 
        linear-gradient(90deg, transparent 0%, rgba(0, 120, 215, 0.05) 25%, rgba(0, 120, 215, 0.05) 75%, transparent 100%),
        linear-gradient(0deg, transparent 0%, rgba(255, 140, 0, 0.04) 50%, transparent 100%);
      background-size: 200% 100%, 100% 200%;
      animation: metro-slide 25s ease-in-out infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes metro-flow {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    @keyframes metro-grid {
      0%, 100% { transform: scale(1) translateY(0); filter: brightness(1); }
      25% { transform: scale(1.02) translateY(-5px); filter: brightness(1.03); }
      50% { transform: scale(1.01) translateY(-10px); filter: brightness(1.05); }
      75% { transform: scale(1.02) translateY(-5px); filter: brightness(1.03); }
    }

    @keyframes metro-slide {
      0%, 100% { background-position: 0% 0%, 0% 0%; }
      50% { background-position: 100% 0%, 0% 100%; }
    }
    
    body > * {
      position: relative;
      z-index: 1;
    }

    @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;800;900&family=Archivo+Black&display=swap');

    .navbar {
      position: sticky;
      top: 0;
      z-index: 1200;
      background: rgba(221, 208, 196, 0.95) !important;
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(45, 90, 79, 0.1) !important;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.08);
      padding: 2rem 0 !important;
      transition: all 0.3s ease;
    }

    .navbar.minimized {
      padding: 0.8rem 0 !important;
    }

    .navbar-brand {
      font-weight: 900;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 1.9rem;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      font-family: 'Archivo Black', sans-serif;
      transition: font-size 0.3s ease;
    }

    .navbar.minimized .navbar-brand {
      font-size: 1.4rem;
    }

    .nav-link {
      color: var(--secondary) !important;
      font-weight: 700;
      transition: all 0.3s ease;
      position: relative;
      font-family: 'Cinzel', serif;
      font-size: 1.05rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin: 0 0.5rem;
      display: flex;
      align-items: center;
    }

    .nav-link::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 0;
      height: 2.5px;
      background: var(--primary);
      transition: width 0.3s ease;
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .post-container {
      padding: 2rem 0;
      max-width: 1000px;
      margin: 0 auto;
    }

    .ig-card {
      border: 0;
      border-radius: 20px;
      background: white;
      overflow: hidden;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
    }

    .ig-header {
      padding: 1.2rem 1.5rem;
      border-bottom: 1px solid #F3F4F6;
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.02) 0%, rgba(6, 214, 160, 0.02) 100%);
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .profile-circle {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      object-fit: cover;
      border: 3px solid var(--accent);
      margin-right: 0.8rem;
    }

    .username-text { 
      font-weight: 800; 
      font-size: 0.95rem;
      color: var(--secondary);
    }

    .post-time {
      font-size: 0.8rem;
      color: #6B7280;
    }

    .ig-image-panel {
      background: linear-gradient(135deg, #F0F9F7 0%, #ECFDF5 100%);
      min-height: 500px;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      padding: 2rem;
    }

    .ig-image-panel img {
      width: 100%;
      height: auto;
      max-height: 600px;
      object-fit: contain;
    }

    .carousel { width: 100%; height: 100%; }
    .carousel-inner { width: 100%; height: 100%; }
    .carousel-item { width: 100%; height: 100%; }
    .carousel .carousel-item img { height: auto; max-height: 600px; object-fit: contain; width: 100%; }

    .post-body {
      padding: 1.5rem;
      border-bottom: 1px solid #F3F4F6;
    }

    .ig-caption {
      font-size: 0.95rem;
      color: var(--secondary);
      line-height: 1.6;
      margin-bottom: 1rem;
    }

    .ig-comments-section {
      padding: 1.5rem;
      max-height: 400px;
      overflow-y: auto;
      background: white;
      min-height: 150px;
    }

    .ig-comment {
      font-size: 0.9rem;
      margin-bottom: 1rem;
      line-height: 1.5;
      color: var(--secondary);
    }

    .ig-comment strong {
      font-weight: 700;
      color: var(--secondary);
      margin-right: 0.5rem;
    }

    .no-comments {
      text-align: center;
      color: #6B7280;
      padding: 2rem 1rem;
    }

    .ig-actions {
      padding: 1rem 1.5rem;
      border-bottom: 1px solid #F3F4F6;
      background: white;
      display: flex;
      gap: 1rem;
      align-items: center;
      justify-content: space-between;
    }

    .ig-like-btn {
      border-radius: 12px;
      font-weight: 700;
      border: 2px solid var(--primary);
      background: white;
      color: var(--primary);
      transition: all 0.3s ease;
      padding: 0.5rem 1rem;
    }

    .ig-like-btn:hover {
      background: var(--primary);
      color: white;
      transform: scale(1.05);
    }

    .ig-like-btn.liked {
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      color: white;
      border-color: var(--primary);
    }

    .ig-add-comment {
      padding: 1.5rem;
      background: white;
      display: flex;
      gap: 0.8rem;
    }

    .ig-add-comment input { 
      border-radius: 12px;
      border: 2px solid var(--border-light);
      padding: 0.75rem 1rem;
      transition: all 0.3s ease;
      flex: 1;
    }

    .ig-add-comment input:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(45, 90, 79, 0.1);
      outline: none;
    }

    .ig-add-comment button {
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      color: white;
      border: none;
      border-radius: 12px;
      font-weight: 700;
      padding: 0.75rem 1.5rem;
      transition: all 0.3s ease;
    }

    .ig-add-comment button:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(45, 90, 79, 0.3);
    }

    .back-link {
      color: var(--primary);
      text-decoration: none;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
      transition: all 0.3s ease;
    }

    .back-link:hover {
      gap: 1rem;
    }

    .link-accent {
      color: var(--primary);
      font-weight: 700;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .link-accent:hover {
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .navbar {
        padding: 1rem 0 !important;
      }

      .navbar-brand {
        font-size: 1.5rem;
      }

      .nav-link {
        font-size: 0.9rem;
        margin: 0.25rem 0;
      }

      .post-container {
        padding: 1rem;
      }

      .ig-image-panel {
        min-height: 300px;
        padding: 1rem;
      }

      .ig-image-panel img {
        max-height: 400px;
      }

      .carousel .carousel-item img {
        max-height: 400px;
      }
    }
  </style>
</head>
<body>
  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/">Clothes <span class="brand-red">Swap</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="mainNavbar">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0" style="display: flex; align-items: center; gap: 0.8rem;">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/inbox">Inbox</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/wardrobe">Wardrobe</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/swap">Swap</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/sahm">SwapMates</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/profile">Profile</a>
            </li>
            <li class="nav-item ms-lg-2">
              <a class="btn btn-sm" style="background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; border: none; border-radius: 10px; font-weight: 700; padding: 0.7rem 1.6rem; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(45, 90, 79, 0.2); font-size: 0.95rem; font-family: 'Cinzel', serif; letter-spacing: 0.08em; text-transform: uppercase; display: flex; align-items: center;" href="/logout" onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 6px 16px rgba(45, 90, 79, 0.3)';" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 4px 12px rgba(45, 90, 79, 0.2)';">Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item ms-lg-2">
              <a class="btn btn-sm" style="background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; border: none; border-radius: 10px; font-weight: 700; padding: 0.7rem 1.6rem; transition: all 0.3s ease; box-shadow: 0 4px 12px rgba(45, 90, 79, 0.2); font-size: 0.95rem; font-family: 'Cinzel', serif; letter-spacing: 0.08em; text-transform: uppercase; display: flex; align-items: center;" href="/login" onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 6px 16px rgba(45, 90, 79, 0.3)';" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 4px 12px rgba(45, 90, 79, 0.2)';">Sign In</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- MAIN CONTENT -->
  <div class="container post-container">
    <a href="/" class="back-link">← Back to Feed</a>

    <% if (message) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <div class="ig-card">
      <!-- HEADER -->
      <div class="ig-header d-flex align-items-center justify-content-between">
        <a href="/users/<%= post.user_id %>" class="d-flex align-items-center text-decoration-none text-dark">
          <% if (post.profile_image_url) { %>
            <img src="<%= post.profile_image_url %>" class="profile-circle me-2" alt="Profile">
          <% } else { %>
            <div class="profile-circle me-2 d-flex align-items-center justify-content-center bg-light text-muted">
              <%= post.username ? post.username.charAt(0).toUpperCase() : '?' %>
            </div>
          <% } %>
          <div>
            <div class="username-text"><%= post.username || 'Unknown' %></div>
            <div class="post-time">
              <%= post.created_at ? new Date(post.created_at).toLocaleString() : '' %>
            </div>
          </div>
        </a>
        <% if (user && user.user_id === post.user_id) { %>
          <div class="d-flex gap-2">
            <a href="/ootd/<%= post.post_id %>/edit" class="btn btn-sm btn-outline-secondary" title="Edit post">✏️</a>
            <form action="/ootd/<%= post.post_id %>/delete" method="POST" style="display:inline;" onsubmit="return confirm('Delete this OOTD post?');">
              <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete post">🗑️</button>
            </form>
          </div>
        <% } %>
      </div>

      <!-- IMAGE SECTION -->
      <div class="ig-image-panel">
        <% 
          const imgList = [];
          if (post.image_url_1) imgList.push(post.image_url_1);
          if (post.image_url_2) imgList.push(post.image_url_2);
          if (post.image_url_3) imgList.push(post.image_url_3);
        %>

        <% if (imgList.length === 0) { %>
          <div class="text-muted">No images</div>
        <% } else if (imgList.length === 1) { %>
          <img src="<%= imgList[0] %>" alt="OOTD">
        <% } else { %>
          <div id="ootdCarousel_<%= post.post_id %>" class="carousel slide w-100" data-bs-ride="carousel">
            <div class="carousel-inner">
              <% imgList.forEach((img, idx) => { %>
                <div class="carousel-item <%= idx === 0 ? 'active' : '' %>">
                  <img src="<%= img %>" alt="OOTD image <%= idx + 1 %>">
                </div>
              <% }) %>
            </div>

            <button class="carousel-control-prev" type="button" data-bs-target="#ootdCarousel_<%= post.post_id %>" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#ootdCarousel_<%= post.post_id %>" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div>
        <% } %>
      </div>

      <!-- CAPTION SECTION -->
      <div class="post-body">
        <% if (post.caption) { %>
          <div class="ig-caption"><%= post.caption %></div>
        <% } else { %>
          <div class="text-muted">No caption.</div>
        <% } %>
      </div>

      <!-- ACTIONS -->
      <div class="ig-actions">
        <div class="d-flex align-items-center gap-3">
          <form action="/ootd/<%= post.post_id %>/like" method="POST" class="d-inline">
            <%
              const isLiked = post && Number(post.liked_by_me) > 0;
            %>
            <button type="submit"
              class="btn btn-sm ig-like-btn <%= isLiked ? 'liked' : '' %>"
              <%= user ? '' : 'disabled' %>>
              ♥ <%= post.like_count || 0 %>
            </button>
          </form>
          <span class="text-muted small">💬 <%= post.comment_count || 0 %></span>
        </div>
      </div>

      <!-- COMMENTS SECTION -->
      <div class="ig-comments-section">
        <% if (post.comments && post.comments.length > 0) { %>
          <% post.comments.forEach(c => { %>
            <div class="ig-comment">
              <strong><a href="/users/<%= c.user_id %>" class="link-accent"><%= c.username %></a></strong>
              <%= c.content %>
              <% if (user && user.user_id === c.user_id) { %>
                <form action="/ootd/comment/<%= c.comment_id %>/delete" method="POST" style="display:inline;" class="ms-2">
                  <button type="submit" class="btn btn-link btn-sm text-danger p-0" title="Delete comment">Delete</button>
                </form>
              <% } %>
            </div>
          <% }) %>
        <% } else { %>
          <div class="no-comments">
            <p>No comments yet. Be the first to comment!</p>
          </div>
        <% } %>
      </div>

      <!-- ADD COMMENT -->
      <div class="ig-add-comment">
        <% if (user) { %>
          <form action="/ootd/<%= post.post_id %>/comment" method="POST" class="d-flex gap-2 w-100">
            <input name="content" class="form-control" placeholder="Add a comment..." maxlength="500" required>
            <button class="btn" style="background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; font-weight: 700; border-radius: 12px;" type="submit">Post</button>
          </form>
        <% } else { %>
          <div class="small">
            <a class="link-accent" href="/login">Sign in</a> to like or comment.
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    // Minimize navbar on scroll
    let lastScrollTop = 0;
    window.addEventListener('scroll', function() {
      let navbar = document.querySelector('.navbar');
      let scrollTop = window.pageYOffset || document.documentElement.scrollTop;
      if (scrollTop > 100) {
        navbar.classList.add('minimized');
      } else {
        navbar.classList.remove('minimized');
      }
      lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;
    });
  </script>
</body>
</html>
